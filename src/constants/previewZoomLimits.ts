/**
 * Bounds for `plantumlViewer.previewZoom` (see `package.json` contributes).
 * Kept in a vscode-free module so unit tests and `previewZoomWheel` do not load `plantumlConfig`.
 */
export const PREVIEW_ZOOM_MIN = 0.25;
export const PREVIEW_ZOOM_MAX = 3;
